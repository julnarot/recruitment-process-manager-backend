spring:
  application:
    name: RPM Discovery server

server:
  port: ${SERVER_PORT:9000}
  
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:9000/eureka}
  server:
    enable-self-preservation: false


